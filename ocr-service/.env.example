PORT=3002
KAFKA_BROKER=kafka:9092
NODE_ENV=production

# OCR Worker Configuration
# Number of worker processes for parallel OCR processing (default: 3)
# Recommended: 2-4 workers for optimal performance
OCR_WORKERS=3
